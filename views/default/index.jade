h2 Projects

ul.projects
  - each project in projects
    li
      a(href: project_path(project), title: 'View project')
        span= project.name || '(Unknown)'
        - if (project.lastBuild())
          - if (project.lastBuild().success == true)
            span(class: 'status success', href: '/projects/' + project.id, title: 'Last build succeeded') success
          - else
            span(class: 'status failure', href: '/projects/' + project.id, title: 'Last build failed') failure

a(href: new_project_path(), title: 'New project') New project
